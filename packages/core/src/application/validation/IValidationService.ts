import { IValidationPipeline } from './IValidationPipeline';

export interface IValidationService extends IValidationPipeline {
  // Extending pipeline logic with specific capabilities if needed in the future
}
